# Adzuna (https://developer.adzuna.com) — set in Vercel project env, never commit real keys
ADZUNA_APP_ID=
ADZUNA_API_KEY=

# Optional: enables cross-device cloud sync (create in Vercel → Storage → Blob)
BLOB_READ_WRITE_TOKEN=
